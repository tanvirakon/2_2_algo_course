int key = rand() % 500 + 1;
    // sort(all(a));
    // int high = size - 1;
    // int low = 0;
    // cout << high << " " << low;
    // while (low <= high)
    // {
    //     int mid = (high + low) / 2;
    //     if (a[mid] == key)
    //     {
    //         flag = 1;
    //         break;
    //     }
    //     if (mid < a[i])
    //         high = mid;
    //     else
    //         low = mid;
    // }
    // ofstream out("soln.txt");
    // out << "binary search: ";
    // if (flag)
    //     out << "yes\n";
    // else
    //     out << "NO\n";